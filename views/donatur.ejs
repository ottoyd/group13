<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <%- include('./navbar.ejs') %><br>
    <h1><%= donatur.nama_donatur %> </h1>
    <form action="/donatur/claim/<%= donatur.id %>" method="POST">
        <input type="text" name="topUp">
        <input type="submit" value="Claim">
    </form>

    <form action="/donatur/topup/<%= donatur.id %> " method="POST">
        <h1>Sando Anda <%= donatur.saldo %></h1>
        <label> Nominal </label>
            <select name="nominal">
                <option value="10000">10000</option>
                <option value="20000">20000</option>
                <option value="50000">50000</option>
                <option value="100000">100000</option>
            </select>
            <input type="submit" value="TOP UP">
    </form> <br>

    <table border>
        <tr>
            <th>nama yayasan</th>
            <th>jumlah donasi</th>
            <th>atas nama</th>
        </tr>
        <h1>HISTORY</h1>
        <% for(let i in data) { %> 
            <% if(data[i].dataValues.DonaturId == donatur.id) {%> 
            <tr>
                <td> <%= data[i].dataValues.Org.nama_yayasan %> </td>
                <td> <%= data[i].dataValues.jumlah_donasi %> </td>
                <td> <%= data[i].dataValues.Donatur.nama_donatur %> </td>
            </tr>
            <% } %> 
        <% } %> 
    </table>
</body>
</html>