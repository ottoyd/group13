<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <table border> 
        <% for(let i in data) { %> 
            <tr>
                <td> <%= data[i].id %> </td>
                <td> <%= data[i].nama_donatur %> </td>
                <td> <%= data[i].email %> </td>
                <td> <%= data[i].password %> </td>
                <td> <%= data[i].saldo %> </td>
                <td> <a href="donatur/goto/<%= data[i].id %>">Go to Page</a> </td>
            </tr>
            <% } %> 
    </table>
</body>
</html>